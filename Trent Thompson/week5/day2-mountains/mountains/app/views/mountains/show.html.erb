<h2> <%=@mountain.name%></h2>

<%= image_tag @mountain.image, :alt => @mountain.name if @mountain.image.present? %>

<ul>
  <li>Name: <%= @mountain.name %> </li>
  <li>Height: <%= @mountain.height %>M </li>
  <li>Mass: <%= number_to_human(@mountain.weight) %> </li>
</ul>

<div class="controls">
  <%= button_to 'Edit mountain', edit_mountain_path(@mountain.id) %>
  <%= button_to 'Delete mountain', mountain_path(@mountain.id), :method => 'delete' %>
</div>
