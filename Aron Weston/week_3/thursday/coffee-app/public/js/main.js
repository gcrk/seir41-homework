$(document).ready(function () {
    // Floating button
    $('.fixed-action-btn').floatingActionButton();
    
    // // Chips for tags
    // const chipsData = JSON.parse($())

    // $('.chips').chips({
    //     data: chipsData
    // });
  

});