<h1>All Beers 🍺</h1>

<ul class="index">

<% @beers.each do |beer| %>

    <li>
      <%= link_to image_tag(beer.image, :class => 'thumb', :alt => beer.name), beer %>
      <p>Name: <%= link_to beer.name, beer %></p>
      <p>Type: <%= beer.variety %></p>
    </li>

<% end %>

</ul>
